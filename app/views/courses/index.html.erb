index.html.erb
<%= session[:cart] %>
<br>
<% @courses.each do |course| %>
  <%= link_to course.name, course_path(course.name) %>
  <br>
<% end %>

<br>
<%= link_to "cartを見る", cart_path %>
<%= link_to "cartをクリア", cart_path, method: :delete %>